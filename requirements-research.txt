-r requirements-prod.txt
scikit-learn>=1.6,<2
scipy>=1.15,<2
xgboost>=2.1,<3
lightgbm>=4.6,<5
# torch is intentionally left out of the default research profile because the
# legacy v5 quantum experiments are not part of the free-tier cloud path.

